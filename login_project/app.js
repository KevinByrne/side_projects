const express = require('express'); /* calls express module with associated functions/objests assigned to it */
const app = express(); /* app is the object which has access to the express methods such as http routing, configuring middleware, rendering HTML views, register a template engine. */

